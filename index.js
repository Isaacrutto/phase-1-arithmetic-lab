// Write your code here
// Define num1 and num2 so that their product equals 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // 31 * 2 = 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Ensures it's always > 0

// Define num3 and num4 so that num3 % num4 equals 4
const num3 = 28;
const num4 = 6;
const mod = num3 % num4; // 28 % 6 = 4

// Find the highest number in a given set
const max = Math.max(1, 5, 10, 15, 20); // 20 is the highest number
